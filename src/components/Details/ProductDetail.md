ProductDetail Component example:

Since the component is a "Modal" component, it will be rendered when clicked on the "Product Card"

```tsx inside Markdown
import '../../assets/styles/bootstrap.min.css'
;<ProductDetail />
```
